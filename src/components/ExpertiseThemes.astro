---
const themes = [
	{
		title: "DevSecOps et Securite",
		icon: "shield",
		color: "red",
		tags: ["CI/CD", "Securite", "Audits", "Automatisation"],
		features: ["Pipelines securises", "Audits de securite", "Outils DevSecOps", "Methodologies agiles"]
	},
	{
		title: "WordPress Pro",
		icon: "globe",
		color: "blue",
		tags: ["WordPress", "Performance", "Plugins", "E-commerce"],
		features: ["Optimisation avancee", "Securisation expert", "Plugins sur mesure", "Solutions e-commerce"]
	},
	{
		title: "Frameworks JavaScript",
		icon: "code-2",
		color: "yellow",
		tags: ["React", "Vue", "Node.js", "TypeScript"],
		features: ["Applications modernes", "API REST/GraphQL", "Architecture scalable", "Tests automatises"]
	},
	{
		title: "SAAS & Architecture Cloud",
		icon: "cloud",
		color: "purple",
		tags: ["SAAS", "AWS", "Docker", "Kubernetes"],
		features: ["Architectures scalables", "Deploiement cloud", "Optimisation couts", "Microservices"]
	},
	{
		title: "Intelligence Artificielle",
		icon: "brain",
		color: "green",
		tags: ["IA", "Machine Learning", "API", "Automatisation"],
		features: ["Integration IA", "Modeles personnalises", "API intelligentes", "Automatisation metier"]
	},
	{
		title: "Formation & Conseil",
		icon: "graduation-cap",
		color: "orange",
		tags: ["Formation", "Conseil", "Audit", "Accompagnement"],
		features: ["Formation technique", "Audit de code", "Conseil strategique", "Accompagnement equipes"]
	}
];
---

<section id="expertise-themes" class="section">
	<div class="container">
		<div class="section-header">
			<h2 class="section-title gradient-text">Domaines d'Expertise</h2>
			<p class="section-description">
				Decouvrez mes competences techniques et les domaines dans lesquels j'interviens pour creer des solutions innovantes et performantes.
			</p>
		</div>
		
		<div class="themes-grid">
			{themes.map((theme) => (
				<div class={`theme-card card theme-${theme.color}`}>
					<div class="theme-header">
						<div class="theme-icon">
							<i data-lucide={theme.icon}></i>
						</div>
						<h3 class="theme-title">{theme.title}</h3>
					</div>
					
					<div class="theme-tags">
						{theme.tags.map((tag) => (
							<span class="tag">{tag}</span>
						))}
					</div>
					
					<div class="theme-features">
						{theme.features.map((feature) => (
							<div class="feature-item">
								<i data-lucide="check"></i>
								<span>{feature}</span>
							</div>
						))}
					</div>
					
					<div class="theme-overlay"></div>
				</div>
			))}
		</div>
	</div>
</section>

<style>
	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-title {
		font-size: clamp(2rem, 5vw, 3rem);
		font-weight: 700;
		margin-bottom: 1rem;
	}

	.section-description {
		font-size: 1.125rem;
		color: hsl(var(--foreground) / 0.8);
		max-width: 600px;
		margin: 0 auto;
	}

	.themes-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
	}

	.theme-card {
		position: relative;
		overflow: hidden;
		cursor: pointer;
		min-height: 320px;
		display: flex;
		flex-direction: column;
	}

	.theme-header {
		display: flex;
		align-items: center;
		gap: 1rem;
		margin-bottom: 1.5rem;
	}

	.theme-icon {
		width: 48px;
		height: 48px;
		border-radius: 12px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: hsl(var(--primary) / 0.1);
		border: 1px solid hsl(var(--primary) / 0.2);
	}

	.theme-icon i {
		width: 24px;
		height: 24px;
		color: hsl(var(--primary));
	}

	.theme-title {
		font-size: 1.25rem;
		font-weight: 600;
	}

	.theme-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
		margin-bottom: 1.5rem;
	}

	.tag {
		padding: 0.25rem 0.75rem;
		background: hsl(var(--muted));
		border: 1px solid hsl(var(--border));
		border-radius: 1rem;
		font-size: 0.75rem;
		color: hsl(var(--foreground) / 0.8);
	}

	.theme-features {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		flex-grow: 1;
	}

	.feature-item {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		font-size: 0.875rem;
		color: hsl(var(--foreground) / 0.8);
	}

	.feature-item i {
		width: 16px;
		height: 16px;
		color: hsl(var(--primary));
	}

	.theme-overlay {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: var(--gradient-primary);
		opacity: 0;
		transition: opacity 0.3s ease;
		pointer-events: none;
	}

	.theme-card:hover .theme-overlay {
		opacity: 0.1;
	}

	/* Theme color variants */
	.theme-red .theme-icon {
		background: hsl(0 84% 60% / 0.1);
		border-color: hsl(0 84% 60% / 0.2);
	}

	.theme-red .theme-icon i,
	.theme-red .feature-item i {
		color: hsl(0 84% 60%);
	}

	.theme-blue .theme-icon {
		background: hsl(217 91% 60% / 0.1);
		border-color: hsl(217 91% 60% / 0.2);
	}

	.theme-blue .theme-icon i,
	.theme-blue .feature-item i {
		color: hsl(217 91% 60%);
	}

	.theme-yellow .theme-icon {
		background: hsl(45 93% 47% / 0.1);
		border-color: hsl(45 93% 47% / 0.2);
	}

	.theme-yellow .theme-icon i,
	.theme-yellow .feature-item i {
		color: hsl(45 93% 47%);
	}

	.theme-purple .theme-icon {
		background: hsl(262 83% 58% / 0.1);
		border-color: hsl(262 83% 58% / 0.2);
	}

	.theme-purple .theme-icon i,
	.theme-purple .feature-item i {
		color: hsl(262 83% 58%);
	}

	.theme-green .theme-icon {
		background: hsl(var(--primary) / 0.1);
		border-color: hsl(var(--primary) / 0.2);
	}

	.theme-orange .theme-icon {
		background: hsl(25 95% 53% / 0.1);
		border-color: hsl(25 95% 53% / 0.2);
	}

	.theme-orange .theme-icon i,
	.theme-orange .feature-item i {
		color: hsl(25 95% 53%);
	}

	@media (max-width: 768px) {
		.themes-grid {
			grid-template-columns: 1fr;
		}
	}
</style>